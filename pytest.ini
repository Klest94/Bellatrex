[pytest]
testpaths = tests
cache_dir = .cache/pytest
markers =
    gui: tests that exercise the NiceGUI and native-window integration
filterwarnings =
    ignore:Could not find the number of physical cores.*:UserWarning:joblib.externals.loky.backend.context
